#include <stdlib.h>
#include <stdio.h>

int _abs(int n);

int main(void)
{
    int n = -10;
    int result;

    result = _abs(n);

    printf("The absolute value of %d is %d\n", n, result);

    return 0;
}
